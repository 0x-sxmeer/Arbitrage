[package]
name = "test_addr"
version = "0.1.0"
edition = "2024"

[dependencies]

# Excluded from the parent Cargo workspace at arb-engine/Cargo.toml.
# This empty [workspace] table makes this nested package self-contained
# so that running `cargo` commands here does not error with
# "current package believes it's in a workspace when it's not".
[workspace]
